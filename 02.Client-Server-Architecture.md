## CLIENT SEVER MODEL ##
PURPOSE:To establish a communication between the client and the server to get the job done.
WE establish protocal that how a client and a server will interact w eo

Example: The bookstore has a cashier(server) coustomer(client)

Where the app is deployed is what we call a SERVER.

## ADVANTAGES ##
1. Central resource management
2. Scalibilty
3. Flexibity of reach

## DISADVANTAGES ##
1. Single point of failure
2. Network Congestion:-
Example: The bookstore has a cashier(server) coustomer(client). No of clients are too much that is making the cashier(server) slow for that we'll hire another cashier(server) so the load gets divided
3. Security Risks

